{
  "name": "pubky-bot-contract",
  "version": "0.1.0",
  "license": "MIT",
  "description": "Implementation-independent behavioral contract for Pubky bots that reply to mentions.",
  "type": "module",
  "types": "./types.d.ts",
  "exports": {
    ".": {
      "types": "./types.d.ts",
      "import": "./types.d.ts",
      "default": "./types.d.ts"
    }
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "record-fixtures": "tsx scripts/record-fixtures.ts",
    "verify:orphans": "tsx scripts/verify-orphan-cleanup.ts"
  },
  "dependencies": {
    "@synonymdev/pubky": "^0.6.0-rc.6",
    "pubky-app-specs": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^5.0.1"
  }
}
